/*

tostring always uses a comma as the separator

join allows specifying a custom separator 


*/

let arr = ["1", "2", 3, false];

console.log(arr.toString());

let numbers = [10, 20, 30, 40];
console.log(numbers.toString());
