/*

pop methods removes the last element of the array and returns it



*/

let arr = [1, 2, 3, 4, false, "Hello"];

let last_elem = arr.pop();

//arr.pop();
//arr.pop();
console.log(last_elem);

console.log(arr);

let num = [10, 20, 30, 40];
let remove = num.pop();
console.log(remove);
console.log(num);
